/**
 * BU DOSYA ARTIK KULLANILMIYOR (Faz 4 cleanup, 2026-05-24).
 *
 * Tüm Supabase Edge Function'ları artık server.ts'de gerçek route'lar olarak port edildi.
 * Bilinmeyen /functions/v1/<fn> request'leri için catch-all 404 inline yapıldı.
 *
 * Bu dosya bir sonraki temizlik turunda `rm` ile silinebilir
 * (auto-mode izin vermediği için boş bırakıldı).
 */
export {};
